name: broadcaster_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  agora_rtc_engine: ^6.3.2
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  permission_handler: ^10.0.0

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

